interface:
  display_name: "Sync Cloud Code"
  short_description: "Sync committed cloud snapshots with content verification"
  default_prompt: "Use $sync-cloud-code to sync the current committed source cloud snapshot into the target repository, verify content and exclusions, and create one traceable target commit."
