interface:
  display_name: "Sync Cloud Code"
  short_description: "Sync cloud code one way, preserving commit boundaries"
  default_prompt: "Use $sync-cloud-code to sync the source cloud directory into the target repository, creating a separate target commit for each relevant source commit."
